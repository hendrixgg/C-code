

int main(int argc, char *argv[])
{
    int line_width = 80;
    char *buffer = NULL;
    /*
    parse the command line arguments, change the default line width if it was specified

    while(true) {
        read a word

        if (can't read word) {
            break;
        }
    }
    */
}